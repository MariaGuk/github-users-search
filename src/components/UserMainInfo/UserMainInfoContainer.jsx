import React from 'react';

import UserMainInfo from './UserMainInfo';

const UserMainInfoContainer = ({ userInfo }) => {
  return (
    <UserMainInfo userInfo={userInfo} />)
};

export default UserMainInfoContainer;